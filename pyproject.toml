[project]
name = "trafficapp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "numpy>=2.5.3",
    "pandas>=3.0.5",
    "requests>=2.34.2",
    "sqlalchemy>=2.0.53",
]

[project.scripts]
trafficapp = "trafficapp:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
